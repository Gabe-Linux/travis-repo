apply plugin: 'java'

task testStuff(type: Exec) {
    println 'Hello Travis from Gradle'
    commandLine 'printenv'

    standardOutput = new ByteArrayOutputStream()

    ext.output = {
        return standardOutput.toString()
    }
}
